Tool dùng go để query và extract reddis database (vibe coding và sửa lại cho vui ==))
Tool dùng trong kịch bản khi cần persistent access trên một redis database kẻ tấn công sẽ tiến hành change TTL của toàn bộ key rồi thực hiện extract toàn bộ key
